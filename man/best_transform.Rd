% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/best_transform.R
\name{best_transform}
\alias{best_transform}
\title{Find the Best Transformation}
\usage{
best_transform(x, method = "auto", all_transforms = FALSE, plot = FALSE)
}
\arguments{
\item{x}{Numeric vector to transform}

\item{method}{Character string specifying the selection method: "auto" (default, based on normality score),
"skewness" (minimal absolute skewness), or "kurtosis" (minimal absolute excess kurtosis)}

\item{all_transforms}{Logical, whether to return all transformations (TRUE) or only the best one (FALSE)}

\item{plot}{Logical, whether to create histogram plots of all transformations}
}
\value{
If all_transforms=TRUE, a list with all transformations, evaluation metrics, and the name of the best transformation;
if all_transforms=FALSE, only the best transformed data
}
\description{
Applies multiple transformation methods to a variable and identifies the best one
based on normality criteria.
}
\examples{
# Generate right-skewed data
set.seed(123)
x <- rexp(100, 0.2)

# Find best transformation
result <- best_transform(x)

# Get all transformations and evaluation
all_results <- best_transform(x, all_transforms = TRUE)
print(all_results$evaluation)

# Plot all transformations
best_transform(x, plot = TRUE)
}
